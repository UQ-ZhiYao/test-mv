async function mpCheckAuth(){try{if(!sb)throw new Error("Supabase not initialised");const{data:data}=await sb.auth.getSession();if(!data.session)return null;return data.session.user}catch(e){return null}}async function mpLoadProfile(userId){const{data:data,error:error}=await sb.from("profiles").select("*").eq("id",userId).single();if(error)throw error;return data}async function mpLoadJointAccountName(jointAccountId){if(!jointAccountId)return null;const{data:data,error:error}=await sb.from("joint_accounts").select("display_name").eq("id",jointAccountId).maybeSingle();if(error)throw error;return data&&data.display_name||null}async function mpLoadJointCoHolders(jointAccountId){if(!jointAccountId)return[];const{data:data,error:error}=await sb.from("profiles").select("full_name, email, status").eq("joint_account_id",jointAccountId).order("full_name",{ascending:true});if(error)throw error;return data||[]}async function mpSaveProfile(userId,updates){const{error:error}=await sb.from("profiles").update(updates).eq("id",userId);if(error)throw error}async function mpLoadNTA(){const pageSize=1e3;let all=[];let page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}all=all.filter(r=>r.date&&r.nta!==null&&r.nta!==undefined);const now=new Date;function bucket(rows){if(!rows.length)return{v:[1,1],l:["—","—"]};if(rows.length===1)return{v:[parseFloat(rows[0].nta),parseFloat(rows[0].nta)],l:[rows[0].date,rows[0].date]};return{v:rows.map(r=>parseFloat(r.nta)),l:rows.map(r=>r.date)}}function since(dateFrom){return all.filter(r=>new Date(r.date)>=dateFrom)}const ytdFrom=new Date(now.getFullYear(),0,1);const oneYFrom=new Date(now);oneYFrom.setFullYear(oneYFrom.getFullYear()-1);const threeYFrom=new Date(now);threeYFrom.setFullYear(threeYFrom.getFullYear()-3);return{ytd:bucket(since(ytdFrom)),"1y":bucket(since(oneYFrom)),"3y":bucket(since(threeYFrom)),all:bucket(all)}}async function mpLoadHoldings(investorId){const[pfRes,inRes]=await Promise.all([sb.from("portfolio").select("instrument_name, product, units, total_cost, vwap_cost, market_value, latest_price, unrealised_pnl"),sb.from("instruments").select("name, ticker, code, product, sector, currency")]);if(pfRes.error)throw pfRes.error;const instByName={};(inRes.data||[]).forEach(function(i){instByName[i.name]=i});const CASH_NAME="MYR Cash Account";const rows=(pfRes.data||[]).map(function(p){const inst=instByName[p.instrument_name]||{};const isCash=p.instrument_name===CASH_NAME;const mv=parseFloat(p.market_value||p.total_cost||0);const tk=(inst.ticker||"").trim();const co=(inst.code||"").trim();const subLine=isCash?"MYR":tk&&co&&tk!==co?tk+" | "+co:tk||co||"—";return{n:p.instrument_name||"Unknown",t:subLine,ticker:tk||"—",code:co||"—",sec:isCash?"Cash":inst.sector||"Unknown",inst:p.product||inst.product||"Other",units:p.units!=null?parseFloat(p.units):null,cost:p.vwap_cost!=null?parseFloat(p.vwap_cost):null,px:p.latest_price!=null?parseFloat(p.latest_price):null,mv:mv,pnl:isCash?0:p.unrealised_pnl!=null?parseFloat(p.unrealised_pnl):0,isCash:isCash}});const nonCash=rows.filter(r=>!r.isCash);const cash=rows.filter(r=>r.isCash);const ordered=nonCash.concat(cash);const totalMV=ordered.reduce((s,r)=>s+(r.mv||0),0);ordered.forEach(function(r){r.al=totalMV>0?r.mv/totalMV*100:0});ordered.sort(function(a,b){if(a.isCash!==b.isCash)return a.isCash?1:-1;return(b.al||0)-(a.al||0)});return ordered}async function mpLoadSecuritiesInstruments(){const{data:data,error:error}=await sb.from("instruments").select("name, ticker, code, product, sector, currency").eq("product","Securities");if(error)throw error;return data||[]}async function mpLoadWatchlist(userId){const{data:data,error:error}=await sb.from("watchlist").select("id, name, code, ticker, created_at").eq("uid",userId).order("created_at",{ascending:true});if(error)throw error;return data||[]}async function mpAddWatchlistItem(userId,inst){const{error:error}=await sb.from("watchlist").insert({uid:userId,name:inst.name,code:inst.code,ticker:inst.ticker});if(error)throw error}async function mpRemoveWatchlistItem(userId,id){const{error:error}=await sb.from("watchlist").delete().eq("uid",userId).eq("id",id);if(error)throw error}async function mpLoadCapitalSummary(userId){const{data:data,error:error}=await sb.from("capital_injection").select("type, amount, units, nta, date, status").eq("uid",userId).eq("status","Approved").order("date",{ascending:true});if(error)throw error;const rows=data||[];const unitsHeld=rows.reduce((s,r)=>s+(parseFloat(r.units)||0),0);let runUnits=0,runCost=0,realizedPnl=0;const cashflows=[];rows.forEach(function(r){const u=parseFloat(r.units)||0;const amt=Math.abs(parseFloat(r.amount)||0);if(u>0){runCost+=amt;runUnits+=u;cashflows.push({date:r.date,amount:-amt})}else if(u<0){const sellUnits=Math.min(Math.abs(u),runUnits);if(runUnits>0&&sellUnits>0){const avgCost=runCost/runUnits;const salePrice=r.nta!=null&&r.nta!==""?parseFloat(r.nta):sellUnits>0?amt/sellUnits:0;realizedPnl+=(salePrice-avgCost)*sellUnits;runUnits-=sellUnits;runCost=runUnits>0?runCost-avgCost*sellUnits:0}cashflows.push({date:r.date,amount:amt})}});const avgCost=runUnits>0?runCost/runUnits:0;const totalCost=avgCost*unitsHeld;return{unitsHeld:unitsHeld,avgCost:avgCost,totalCost:totalCost,realizedPnl:realizedPnl,cashflows:cashflows}}async function mpLoadTransactions(userId){const{data:data,error:error}=await sb.from("capital_injection").select("reference_id, type, amount, units, nta, date, status, document").eq("uid",userId).order("date",{ascending:false});if(error)throw error;return(data||[]).map(function(t){const units=parseFloat(t.units)||0;const amt=parseFloat(t.amount)||0;return{ref:t.reference_id||"—",type:t.type,date:formatDate(t.date),dateRaw:t.date,units:t.type==="Redemption"?"−"+Math.abs(units).toFixed(4):"+"+Math.abs(units).toFixed(4),unitsRaw:t.type==="Redemption"?-Math.abs(units):Math.abs(units),nav:t.nta!=null?parseFloat(t.nta).toFixed(4):"—",amt:t.type==="Redemption"?"−RM "+Math.abs(amt).toLocaleString("en-MY",{minimumFractionDigits:2}):"+RM "+Math.abs(amt).toLocaleString("en-MY",{minimumFractionDigits:2}),amtRaw:t.type==="Redemption"?-Math.abs(amt):Math.abs(amt),status:t.status||"Pending",doc:t.document||null}})}async function mpLoadDistributions(userId){const[distRes,ciRes]=await Promise.all([sb.from("distributions").select("fy, type, ex_date, pay_date, dps, status").order("ex_date",{ascending:false}),sb.from("capital_injection").select("units, date").eq("uid",userId).eq("status","Approved").order("date",{ascending:true})]);if(distRes.error)throw distRes.error;const dists=distRes.data||[];const myCI=ciRes.data||[];return dists.map(function(d){const exDate=d.ex_date;let myUnits=0;myCI.forEach(function(r){if(r.date&&exDate&&r.date<=exDate)myUnits+=parseFloat(r.units)||0});myUnits=Math.max(0,myUnits);const dps=parseFloat(d.dps)||0;const myAmount=myUnits>0?myUnits*(dps/100):0;return{fy:d.fy,type:d.type,ex:formatDate(d.ex_date),pay:formatDate(d.pay_date),exRaw:d.ex_date,payRaw:d.pay_date,dps:dps.toFixed(2),units:myUnits,amt:myAmount,status:d.status||"Pending"}})}async function mpLoadDocuments(){const{data:data,error:error}=await sb.from("documents").select("*").order("document_date",{ascending:false});if(error)throw error;return(data||[]).map(function(d){return{n:d.title,type:d.document_type,date:formatDate(d.document_date),sz:d.file_size||"—",url:d.file_url||null}})}async function mpLoadNominees(investorId){const{data:data,error:error}=await sb.from("nominees").select("*").eq("investor_id",investorId).order("is_primary",{ascending:false});if(error)throw error;return(data||[]).map(function(n){return{id:n.id,name:n.full_name,rel:n.relationship,nric:n.nric_passport,dob:n.date_of_birth,mob:n.mobile,alloc:parseFloat(n.allocation_pct)}})}async function mpSaveNominee(investorId,nominee){if(nominee.id){const{error:error}=await sb.from("nominees").update(nominee).eq("id",nominee.id);if(error)throw error}else{const{error:error}=await sb.from("nominees").insert({...nominee,investor_id:investorId});if(error)throw error}}async function mpDeleteNominee(nomineeId){const{error:error}=await sb.from("nominees").delete().eq("id",nomineeId);if(error)throw error}async function mpSubmitCapitalInjectionRequest(uid,{fullName:fullName,type:type,amount:amount,document:document,referenceId:referenceId}){const{error:error}=await sb.from("capital_injection").insert({uid:uid,full_name:fullName||null,date:(new Date).toISOString().slice(0,10),type:type,amount:parseFloat(amount),nta:null,units:null,status:"Pending",document:document||null,reference_id:referenceId||null});if(error)throw error}async function mpLoadAdminBankAccount(){const{data:data,error:error}=await sb.from("profiles").select("bank_name, bank_account_no, bank_account_holder").ilike("role","admin").limit(1);if(error)throw error;return data&&data[0]||null}async function mpCountCapitalInjectionRequests(uid,type){const{count:count,error:error}=await sb.from("capital_injection").select("id",{count:"exact",head:true}).eq("uid",uid).eq("type",type);if(error)throw error;return count||0}async function mpUploadReceipt(uid,file){const ext=(file.name.split(".").pop()||"bin").toLowerCase();const path=uid+"/"+Date.now()+"."+ext;const{error:error}=await sb.storage.from("capital-injection-docs").upload(path,file);if(error)throw error;const{data:data}=sb.storage.from("capital-injection-docs").getPublicUrl(path);return data.publicUrl}async function mpLoadShareholders(){const{data:data,error:error}=await sb.from("capital_injection").select("uid, full_name, units, date, status").eq("status","Approved");if(error)throw error;const byUid={};(data||[]).forEach(function(r){if(!byUid[r.uid])byUid[r.uid]={uid:r.uid,full_name:r.full_name,units_held:0,firstDate:r.date};byUid[r.uid].units_held+=parseFloat(r.units)||0;if(r.date&&(!byUid[r.uid].firstDate||r.date<byUid[r.uid].firstDate))byUid[r.uid].firstDate=r.date});return Object.keys(byUid).map(function(uid){const s=byUid[uid];return{investor_id:s.uid,full_name:s.full_name||"Unknown",account_type:"shareholder",joined_date:s.firstDate,status:"Active",units_held:s.units_held}}).sort(function(a,b){return b.units_held-a.units_held})}async function mpLoadShareholdersByFy(){const[fyRes,ciRes]=await Promise.all([sb.from("fy_settings").select("*").order("start_date",{ascending:true}),sb.from("capital_injection").select("uid, full_name, units, date, status").eq("status","Approved")]);if(fyRes.error)throw fyRes.error;if(ciRes.error)throw ciRes.error;const FYS=fyRes.data||[];const ciRows=(ciRes.data||[]).slice().sort(function(a,b){return(a.date||"").localeCompare(b.date||"")});return FYS.map(function(fy){const byUid={};ciRows.forEach(function(r){if(!r.date||r.date>fy.end_date)return;if(!byUid[r.uid])byUid[r.uid]={uid:r.uid,full_name:r.full_name,units_held:0,firstDate:r.date};byUid[r.uid].units_held+=parseFloat(r.units)||0;if(r.date<byUid[r.uid].firstDate)byUid[r.uid].firstDate=r.date});const list=Object.keys(byUid).map(function(uid){const s=byUid[uid];return{investor_id:s.uid,full_name:s.full_name||"Unknown",account_type:"shareholder",joined_date:s.firstDate,status:"Active",units_held:s.units_held}}).filter(function(s){return s.units_held>0}).sort(function(a,b){return b.units_held-a.units_held});return{fy:fy.label,list:list}})}async function mpLoadProductTypes(){const{data:data,error:error}=await sb.from("product_types").select("name, color, bg_color");if(error)throw error;const map={};(data||[]).forEach(function(p){map[p.name]={color:p.color,bg:p.bg_color}});return map}async function mpLoadNtaMonthly(){const pageSize=1e3;let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}const byMonth={};const order=[];all.forEach(function(r){if(!r.date||r.nta==null)return;const key=r.date.slice(0,7);const v=parseFloat(r.nta);if(!byMonth[key]){byMonth[key]={key:key,open:v,high:v,low:v,close:v};order.push(key)}else{const m=byMonth[key];m.high=Math.max(m.high,v);m.low=Math.min(m.low,v);m.close=v}});return order.map(function(key){const m=byMonth[key];const d=new Date(key+"-01T00:00:00");const label=d.toLocaleDateString("en-MY",{month:"short",year:"2-digit"});return{key:key,date:key+"-01",label:label,open:m.open,high:m.high,low:m.low,close:m.close}})}async function mpLoadInceptionDate(){const{data:data,error:error}=await sb.from("capital_injection").select("date").order("date",{ascending:true}).limit(1);if(error)throw error;return data&&data.length?data[0].date:null}async function mpLoadNtaDaily(){const pageSize=1e3;let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}return all.filter(function(r){return r.date&&r.nta!=null}).map(function(r){return{date:r.date,nta:parseFloat(r.nta)}})}async function mpLoadNtaWeeklyOHLC(){const pageSize=1e3;let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}const byWeek={};const order=[];all.forEach(function(r){if(!r.date||r.nta==null)return;const key=isoWeekKey(new Date(r.date+"T00:00:00"));const v=parseFloat(r.nta);if(!byWeek[key]){byWeek[key]={key:key,date:r.date,open:v,high:v,low:v,close:v};order.push(key)}else{const w=byWeek[key];w.high=Math.max(w.high,v);w.low=Math.min(w.low,v);w.close=v;w.date=r.date}});return order.map(function(key){const w=byWeek[key];const d=new Date(w.date+"T00:00:00");const label=d.toLocaleDateString("en-MY",{day:"numeric",month:"short",year:"2-digit"});return{key:key,date:w.date,label:label,open:w.open,high:w.high,low:w.low,close:w.close}})}async function mpLoadNtaQuarterlyOHLC(){const pageSize=1e3;let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}const byQuarter={};const order=[];all.forEach(function(r){if(!r.date||r.nta==null)return;const d=new Date(r.date+"T00:00:00");const q=Math.floor(d.getMonth()/3)+1;const key=d.getFullYear()+"-Q"+q;const v=parseFloat(r.nta);if(!byQuarter[key]){byQuarter[key]={key:key,date:r.date,open:v,high:v,low:v,close:v};order.push(key)}else{const qq=byQuarter[key];qq.high=Math.max(qq.high,v);qq.low=Math.min(qq.low,v);qq.close=v;qq.date=r.date}});return order.map(function(key){const qq=byQuarter[key];return{key:key,date:qq.date,label:key,open:qq.open,high:qq.high,low:qq.low,close:qq.close}})}function isoWeekKey(d){const dt=new Date(Date.UTC(d.getFullYear(),d.getMonth(),d.getDate()));const day=(dt.getUTCDay()+6)%7;dt.setUTCDate(dt.getUTCDate()-day+3);const firstThursday=new Date(Date.UTC(dt.getUTCFullYear(),0,4));const week=1+Math.round(((dt-firstThursday)/864e5-3+(firstThursday.getUTCDay()+6)%7)/7);return dt.getUTCFullYear()+"-W"+String(week).padStart(2,"0")}async function mpLoadNtaWeekly(){const pageSize=1e3;let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}const byWeek={};const order=[];let firstKey=null;all.forEach(function(r){if(!r.date||r.nta==null)return;const key=isoWeekKey(new Date(r.date+"T00:00:00"));const v=parseFloat(r.nta);if(firstKey===null)firstKey=key;if(!byWeek[key]){byWeek[key]={date:r.date,close:v};order.push(key)}else if(key!==firstKey){byWeek[key].date=r.date;byWeek[key].close=v}});return order.map(function(key){return byWeek[key]})}async function mpLoadYahooWeekly(symbol){const{data:data,error:error}=await sb.functions.invoke("fetch-historical",{body:{symbol:symbol,interval:"1wk",range:"5y"}});if(error)throw new Error("fetch-historical failed for "+symbol+": "+error.message);if(data&&data.error)throw new Error(data.error);return(data&&data.points||[]).map(function(p){return{date:p.date,close:p.close}})}async function mpLoadHistorical(symbol,interval,range){const{data:data,error:error}=await sb.functions.invoke("fetch-historical",{body:{symbol:symbol,interval:interval||"1wk",range:range||"5y"}});if(error)throw new Error("fetch-historical failed for "+symbol+": "+error.message);if(data&&data.error)throw new Error(data.error);return(data&&data.points||[]).map(function(p){return{date:p.date,close:p.close}})}async function mpLoadQuotes(symbols){const{data:data,error:error}=await sb.functions.invoke("fetch-quotes",{body:{symbols:symbols}});if(error)throw new Error("fetch-quotes failed: "+error.message);if(data&&data.error)throw new Error(data.error);return data&&data.quotes||[]}async function mpSendFeedback(subject,content,memberEmail,memberName){const{data:data,error:error}=await sb.functions.invoke("send-feedback",{body:{subject:subject,content:content,memberEmail:memberEmail,memberName:memberName}});if(error){var detail=error.message||"unknown error";if(error.context&&typeof error.context.json==="function"){try{var errBody=await error.context.json();if(errBody&&errBody.error)detail=errBody.error}catch(_e){}}throw new Error(detail)}if(data&&data.error)throw new Error(data.error);return true}async function mpLoadDistributionsByFy(){const{data:data,error:error}=await sb.from("distributions").select("fy, type, dps, status").eq("status","Paid");if(error)throw error;const rows=data||[];const byFy={};const order=[];rows.forEach(function(r){const fy=r.fy;if(!fy)return;if(!byFy[fy]){byFy[fy]={fy:fy,interimDps:0,finalDps:0};order.push(fy)}const dps=parseFloat(r.dps)||0;const isInterim=(r.type||"").toLowerCase().indexOf("interim")!==-1;if(isInterim)byFy[fy].interimDps+=dps;else byFy[fy].finalDps+=dps});order.sort();return order.map(function(fy){const r=byFy[fy];r.totalDps=r.interimDps+r.finalDps;return r})}async function mpLoadFundOverview(){const{data:data,error:error}=await sb.from("fund_overview").select("*").single();if(error)throw error;return data}async function mpLoadIncomeStatement(){const pageSize=1e3;async function fetchAllNta(){let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, total_equity, management_fees").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}return all}const[fyRes,divRes,otherRes,remRes,settlRes,ciRes,distRes,ntaRows]=await Promise.all([sb.from("fy_settings").select("*").order("start_date",{ascending:true}),sb.from("dividend").select("amount, ex_date, status"),sb.from("transaction_others").select("amount, date, category"),sb.from("remuneration").select("amount, date"),sb.from("settlement").select("pnl, date"),sb.from("capital_injection").select("amount, units, date, status").eq("status","Approved"),sb.from("distributions").select("amount, pay_date, status"),fetchAllNta()]);if(fyRes.error)throw fyRes.error;[["dividend",divRes],["transaction_others",otherRes],["remuneration",remRes],["settlement",settlRes],["capital_injection",ciRes],["distributions",distRes]].forEach(function(pair){if(pair[1].error)console.warn('Income statement: "'+pair[0]+'" query failed — '+pair[1].error.message)});const FYS=fyRes.data||[];const divRows=divRes.data||[];const otherRows=otherRes.data||[];const remRows=remRes.data||[];const settlRows=settlRes.data||[];const ciRows=ciRes.data||[];const distRows=distRes.data||[];function ntaAtOrBefore(dateStr){let result=null;for(let i=0;i<ntaRows.length;i++){if(ntaRows[i].date<=dateStr)result=ntaRows[i];else break}return result}function inRange(d,start,end){return d&&d>=start&&d<=end}function isInterest(cat){return(cat||"").trim().toLowerCase()==="interests"}let cumNetIncomeBefore=0;const rows=FYS.map(function(fy){const start=fy.start_date,end=fy.end_date;const dividendIncome=divRows.filter(function(r){return r.status==="Received"&&inRange(r.ex_date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const interestIncome=otherRows.filter(function(r){return isInterest(r.category)&&inRange(r.date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const revenue=dividendIncome+interestIncome;const ntaEnd=ntaAtOrBefore(end);const ntaStart=ntaAtOrBefore(start);const mgmtFeeDelta=(ntaEnd?parseFloat(ntaEnd.management_fees)||0:0)-(ntaStart?parseFloat(ntaStart.management_fees)||0:0);const remInFY=remRows.filter(function(r){return inRange(r.date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const managementCost=mgmtFeeDelta+remInFY;const grossIncome=revenue-managementCost;const realizedPnl=settlRows.filter(function(r){return inRange(r.date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.pnl)||0)},0);const otherIncomeExpense=otherRows.filter(function(r){return!isInterest(r.category)&&inRange(r.date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const totalEquityEnd=ntaEnd?parseFloat(ntaEnd.total_equity)||0:0;const totalCapitalEnd=ciRows.filter(function(r){return r.date<=end}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const cumDistPaidThroughEnd=distRows.filter(function(r){return r.status==="Paid"&&r.pay_date&&r.pay_date<=end}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const netIncomeFromEquity=totalEquityEnd-totalCapitalEnd-cumNetIncomeBefore+cumDistPaidThroughEnd;const unrealizedPnl=netIncomeFromEquity-grossIncome-realizedPnl-otherIncomeExpense;const profitBeforeTax=grossIncome+realizedPnl+unrealizedPnl+otherIncomeExpense;const tax=0;const netIncome=profitBeforeTax-tax;const outstandingShares=ciRows.filter(function(r){return r.date<=end}).reduce(function(s,r){return s+(parseFloat(r.units)||0)},0);const epsCents=outstandingShares>0?netIncome/outstandingShares*100:null;cumNetIncomeBefore+=netIncome;return{fy:fy.label,startDate:start,endDate:end,dividendIncome:dividendIncome,interestIncome:interestIncome,revenue:revenue,managementCost:managementCost,grossIncome:grossIncome,realizedPnl:realizedPnl,unrealizedPnl:unrealizedPnl,otherIncomeExpense:otherIncomeExpense,profitBeforeTax:profitBeforeTax,tax:tax,netIncome:netIncome,outstandingShares:outstandingShares,epsCents:epsCents}});return rows}async function mpLoadBalanceSheet(){const pageSize=1e3;async function fetchAllNta(){let all=[],page=0;while(true){const{data:data,error:error}=await sb.from("nta_daily").select("date, securities, other_assets, receivables, cash, management_fees, total_equity, total_units, nta").order("date",{ascending:true}).range(page*pageSize,page*pageSize+pageSize-1);if(error)throw error;if(!data||!data.length)break;all=all.concat(data);if(data.length<pageSize)break;page++}return all}const[fyRes,ciRes,ntaRows]=await Promise.all([sb.from("fy_settings").select("*").order("start_date",{ascending:true}),sb.from("capital_injection").select("amount, date, status").eq("status","Approved"),fetchAllNta()]);if(fyRes.error)throw fyRes.error;const FYS=fyRes.data||[];const ciRows=ciRes.data||[];function ntaAtOrBefore(dateStr){let result=null;for(let i=0;i<ntaRows.length;i++){if(ntaRows[i].date<=dateStr)result=ntaRows[i];else break}return result}const rows=FYS.map(function(fy){const end=fy.end_date;const row=ntaAtOrBefore(end)||{};const securities=parseFloat(row.securities)||0;const otherInvestments=parseFloat(row.other_assets)||0;const dividendReceivables=parseFloat(row.receivables)||0;const cash=parseFloat(row.cash)||0;const totalAssets=securities+otherInvestments+dividendReceivables+cash;const accrualFees=parseFloat(row.management_fees)||0;const totalLiabilities=accrualFees;const totalCapital=ciRows.filter(function(r){return r.date<=end}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const totalEquity=parseFloat(row.total_equity)||0;const retainedEarnings=totalEquity-totalCapital;const outstandingShares=parseFloat(row.total_units)||0;const ntaPerShare=parseFloat(row.nta)||0;return{fy:fy.label,endDate:end,securities:securities,otherInvestments:otherInvestments,dividendReceivables:dividendReceivables,cash:cash,totalAssets:totalAssets,accrualFees:accrualFees,totalLiabilities:totalLiabilities,totalCapital:totalCapital,retainedEarnings:retainedEarnings,totalEquity:totalEquity,outstandingShares:outstandingShares,ntaPerShare:ntaPerShare}});return rows}async function mpLoadCashFlow(incomeStatementRows,balanceSheetRows){const[fyRes,ciRes,distRes,tradeRes]=await Promise.all([sb.from("fy_settings").select("*").order("start_date",{ascending:true}),sb.from("capital_injection").select("amount, date, status").eq("status","Approved"),sb.from("distributions").select("amount, pay_date, status"),sb.from("transaction_trading").select("cashflow, product, trade_date")]);if(fyRes.error)throw fyRes.error;[["capital_injection",ciRes],["distributions",distRes],["transaction_trading",tradeRes]].forEach(function(pair){if(pair[1].error)console.warn('Cash flow: "'+pair[0]+'" query failed — '+pair[1].error.message)});const FYS=fyRes.data||[];const ciRows=ciRes.data||[];const distRows=distRes.data||[];const tradeRows=tradeRes.data||[];function inRange(d,start,end){return d&&d>=start&&d<=end}function isSecuritiesProduct(p){p=(p||"").trim();return p==="Securities"||p==="REIT Trusts"}const isByFy={};(incomeStatementRows||[]).forEach(function(r){isByFy[r.fy]=r});const bsByFy={};(balanceSheetRows||[]).forEach(function(r){bsByFy[r.fy]=r});let seedCash=0;if(FYS.length){const{data:data}=await sb.from("nta_daily").select("date, cash").lte("date",FYS[0].start_date).order("date",{ascending:false}).limit(1);if(data&&data.length)seedCash=parseFloat(data[0].cash)||0}let prevReceivables=null,prevAccrualFees=null,prevCashEnd=null;const rows=FYS.map(function(fy){const start=fy.start_date,end=fy.end_date;const is=isByFy[fy.label]||{};const bs=bsByFy[fy.label]||{};const profitBeforeTax=is.profitBeforeTax||0;const unrealizedAdjustment=-(is.unrealizedPnl||0);const realizedAdjustment=-(is.realizedPnl||0);const fyTrades=tradeRows.filter(function(r){return inRange(r.trade_date,start,end)});const proceedsSecurities=fyTrades.filter(function(r){return isSecuritiesProduct(r.product)}).reduce(function(s,r){return s+(parseFloat(r.cashflow)||0)},0);const proceedsOtherAssets=fyTrades.filter(function(r){return!isSecuritiesProduct(r.product)}).reduce(function(s,r){return s+(parseFloat(r.cashflow)||0)},0);const receivables=bs.dividendReceivables||0;const changeReceivables=(prevReceivables===null?0:prevReceivables)-receivables;const accrualFees=bs.accrualFees||0;const changeAccrualFees=accrualFees-(prevAccrualFees===null?0:prevAccrualFees);const cashflowFromOps=profitBeforeTax+unrealizedAdjustment+realizedAdjustment+proceedsSecurities+proceedsOtherAssets+changeReceivables+changeAccrualFees;const incomeTaxPaid=0;const netCashOperating=cashflowFromOps+incomeTaxPaid;const netCashInvesting=0;const dividendPaid=-distRows.filter(function(r){return r.status==="Paid"&&inRange(r.pay_date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const issuanceOfShares=ciRows.filter(function(r){return inRange(r.date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.amount)||0)},0);const netCashFinancing=dividendPaid+issuanceOfShares;const netIncreaseInCash=netCashOperating+netCashInvesting+netCashFinancing;const cashBeginning=prevCashEnd===null?seedCash:prevCashEnd;const cashEnding=cashBeginning+netIncreaseInCash;prevReceivables=receivables;prevAccrualFees=accrualFees;prevCashEnd=cashEnding;return{fy:fy.label,startDate:start,endDate:end,profitBeforeTax:profitBeforeTax,unrealizedAdjustment:unrealizedAdjustment,realizedAdjustment:realizedAdjustment,proceedsSecurities:proceedsSecurities,proceedsOtherAssets:proceedsOtherAssets,changeReceivables:changeReceivables,changeAccrualFees:changeAccrualFees,cashflowFromOps:cashflowFromOps,incomeTaxPaid:incomeTaxPaid,netCashOperating:netCashOperating,netCashInvesting:netCashInvesting,dividendPaid:dividendPaid,issuanceOfShares:issuanceOfShares,netCashFinancing:netCashFinancing,netIncreaseInCash:netIncreaseInCash,cashBeginning:cashBeginning,cashEnding:cashEnding}});return rows}async function mpLoadHoldingsByFy(){const[fyRes,trRes,inRes]=await Promise.all([sb.from("fy_settings").select("*").order("start_date",{ascending:true}),sb.from("transaction_trading").select("*").order("trade_date",{ascending:true}),sb.from("instruments").select("name, sector, product, ticker, code")]);if(fyRes.error)throw fyRes.error;if(trRes.error)throw trRes.error;const FYS=fyRes.data||[];const trades=trRes.data||[];const instByName={};(inRes.data||[]).forEach(function(i){instByName[i.name]=i});function nameOf(r){return r.instrument_name||r.name||r.security_name||r.instrument||r.code||r.ticker||r.product||"Unknown"}function isCashName(n){return(n||"").toLowerCase().indexOf("cash")!==-1}return FYS.map(function(fy){const byName={};trades.forEach(function(r){if(!r.trade_date||r.trade_date>fy.end_date)return;const name=nameOf(r);if(isCashName(name))return;if(!byName[name])byName[name]={name:name,units:0,lastPrice:null,lastDate:null};byName[name].units+=parseFloat(r.units)||0;const p=parseFloat(r.price);if(!isNaN(p)&&(!byName[name].lastDate||r.trade_date>=byName[name].lastDate)){byName[name].lastPrice=p;byName[name].lastDate=r.trade_date}});const holdings=Object.keys(byName).map(function(name){const h=byName[name];const inst=instByName[name]||{};const mv=(h.units||0)*(h.lastPrice||0);return{name:name,sector:inst.sector||"Other",product:inst.product||"Other",ticker:(inst.ticker||"").trim(),code:(inst.code||"").trim(),units:h.units,price:h.lastPrice,mv:mv}}).filter(function(h){return Math.abs(h.units)>1e-4});const totalMV=holdings.reduce(function(s,h){return s+Math.max(0,h.mv)},0);holdings.forEach(function(h){h.pct=totalMV>0?Math.max(0,h.mv)/totalMV*100:0});holdings.sort(function(a,b){return b.pct-a.pct});return{fy:fy.label,totalMV:totalMV,holdings:holdings}})}async function mpLoadRatioAnalysis(incomeStatementRows,balanceSheetRows){const[fyRes,distRes,tradeRes]=await Promise.all([sb.from("fy_settings").select("*").order("start_date",{ascending:true}),sb.from("distributions").select("dps, ex_date, status"),sb.from("transaction_trading").select("product, trade_date, units, price, fee")]);if(fyRes.error)throw fyRes.error;const FYS=fyRes.data||[];const distRows=distRes.data||[];const tradeRows=tradeRes.data||[];function inRange(d,start,end){return d&&d>=start&&d<=end}function isCashFunds(p){return(p||"").trim().toLowerCase()==="cash funds"}const isByFy={};(incomeStatementRows||[]).forEach(function(r){isByFy[r.fy]=r});const bsByFy={};(balanceSheetRows||[]).forEach(function(r){bsByFy[r.fy]=r});let prevTotalAssets=null;const rows=FYS.map(function(fy){const start=fy.start_date,end=fy.end_date;const is=isByFy[fy.label]||{};const bs=bsByFy[fy.label]||{};const revenue=is.revenue||0;const grossIncome=is.grossIncome||0;const profitBeforeTax=is.profitBeforeTax||0;const netIncome=is.netIncome||0;const totalAssets=bs.totalAssets||0;const totalLiabilities=bs.totalLiabilities||0;const totalEquity=bs.totalEquity||0;const cash=bs.cash||0;const ntaPerShare=bs.ntaPerShare||0;const grossMargin=revenue?grossIncome/revenue*100:null;const pbtMargin=revenue?profitBeforeTax/revenue*100:null;const natMargin=revenue?netIncome/revenue*100:null;const avgAssets=prevTotalAssets===null?totalAssets:(totalAssets+prevTotalAssets)/2;const yieldReturn=avgAssets?revenue/avgAssets*100:null;const grossReturn=avgAssets?grossIncome/avgAssets*100:null;const returnOnAsset=avgAssets?netIncome/avgAssets*100:null;const gearingRatio=totalEquity?totalLiabilities/totalEquity*100:null;const cashReserveRatio=totalAssets?cash/totalAssets*100:null;const dps=distRows.filter(function(r){return r.status==="Paid"&&inRange(r.ex_date,start,end)}).reduce(function(s,r){return s+(parseFloat(r.dps)||0)},0);const dividendYield=ntaPerShare?dps/100/ntaPerShare*100:null;const fyTrades=tradeRows.filter(function(r){return inRange(r.trade_date,start,end)&&!isCashFunds(r.product)});const numTransactions=fyTrades.length;const totalTradingAmount=fyTrades.reduce(function(s,r){return s+Math.abs((parseFloat(r.units)||0)*(parseFloat(r.price)||0))},0);const totalTradingUnits=fyTrades.reduce(function(s,r){return s+Math.abs(parseFloat(r.units)||0)},0);const totalTradingFees=fyTrades.reduce(function(s,r){return s+(parseFloat(r.fee)||0)},0);const feesRate=totalTradingAmount?totalTradingFees/totalTradingAmount*100:null;prevTotalAssets=totalAssets;return{fy:fy.label,startDate:start,endDate:end,grossMargin:grossMargin,pbtMargin:pbtMargin,natMargin:natMargin,yieldReturn:yieldReturn,grossReturn:grossReturn,returnOnAsset:returnOnAsset,gearingRatio:gearingRatio,cashReserveRatio:cashReserveRatio,dps:dps,dividendYield:dividendYield,numTransactions:numTransactions,totalTradingAmount:totalTradingAmount,totalTradingUnits:totalTradingUnits,totalTradingFees:totalTradingFees,feesRate:feesRate}});return rows}async function mpUpdatePassword(newPassword){const{error:error}=await sb.auth.updateUser({password:newPassword});if(error)throw error}async function mpLoadNotifications(investorId){const{data:data,error:error}=await sb.from("notifications").select("*").eq("investor_id",investorId).order("created_at",{ascending:false}).limit(20);if(error)return[];return data||[]}function formatDate(iso){if(!iso)return"—";const d=new Date(iso);return d.toLocaleDateString("en-MY",{day:"numeric",month:"short",year:"numeric"})}async function mpLoadAll(user,investorId){const[profile,holdings,transactions,distributions,documents,nominees,notifications]=await Promise.allSettled([mpLoadProfile(user.id),mpLoadHoldings(investorId),mpLoadTransactions(user.id),mpLoadDistributions(user.id),mpLoadDocuments(),mpLoadNominees(investorId),mpLoadNotifications(investorId)]);return{profile:profile.value||null,holdings:holdings.value||[],transactions:transactions.value||[],distributions:distributions.value||[],documents:documents.value||[],nominees:nominees.value||[],notifications:notifications.value||[]}}